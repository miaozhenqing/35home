# 端口统一和配置优化实现计划

## [x] 任务1：创建前端API配置文件
- **优先级**：P0
- **依赖**：无
- **描述**：在前端创建一个统一的API配置文件，管理API基础URL和端口配置
- **成功标准**：创建 `src/api/config.js` 文件，包含统一的API基础URL配置
- **测试要求**：
  - `programmatic` TR-1.1：文件成功创建且包含正确的配置结构
  - `human-judgement` TR-1.2：配置文件结构清晰，易于维护

## [x] 任务2：修改后端配置文件
- **优先级**：P0
- **依赖**：无
- **描述**：在后端配置中添加API基础URL配置，确保前后端端口一致
- **成功标准**：后端配置文件包含API基础URL配置，默认端口为9001
- **测试要求**：
  - `programmatic` TR-2.1：配置文件能正确加载环境变量
  - `human-judgement` TR-2.2：配置结构合理，包含必要的字段

## [x] 任务3：更新前端Login.vue和Register.vue
- **优先级**：P1
- **依赖**：任务1
- **描述**：修改Login.vue和Register.vue，使用统一的API配置文件替代硬编码的URL
- **成功标准**：两个文件都使用统一的API配置，移除硬编码的URL
- **测试要求**：
  - `programmatic` TR-3.1：文件能正确导入和使用API配置
  - `human-judgement` TR-3.2：代码结构清晰，易于维护

## [x] 任务4：更新前端Home.vue
- **优先级**：P1
- **依赖**：任务1
- **描述**：修改Home.vue，将端口从8000改为9001，并使用统一的API配置
- **成功标准**：Home.vue使用正确的端口9001并使用统一的API配置
- **测试要求**：
  - `programmatic` TR-4.1：文件能正确导入和使用API配置
  - `human-judgement` TR-4.2：代码结构清晰，易于维护

## [x] 任务5：测试服务器启动和API调用
- **优先级**：P1
- **依赖**：任务1-4
- **描述**：启动后端服务器和前端开发服务器，测试API调用是否正常
- **成功标准**：服务器能正常启动，API调用能成功连接
- **测试要求**：
  - `programmatic` TR-5.1：后端服务器在9001端口正常启动
  - `programmatic` TR-5.2：前端能成功调用后端API
  - `human-judgement` TR-5.3：页面功能正常，无控制台错误

## 风险和注意事项
- 确保环境变量配置正确，避免部署时出现端口不匹配
- 修改代码时注意保持向后兼容性
- 测试时需要同时启动前后端服务